export PATH="$PATH:$HOME/.shellac/bin"


if [[ "$OSTYPE" == *"darwin"* ]] then
  export PATH="$PATH:$HOME/.shellac/bin/macos"
fi

if [[ "$OSTYPE" == *"linux"* ]] then
  export PATH="$PATH:$HOME/.shellac/bin/linux"
fi

alias ls="ls -lta"
